spring:
  # 数据库源配置
  datasource:
    # 驱动器类名
    driver-class-name: org.h2.Driver
    # 连接协议:数据库管理系统:内存模式:数据库地址
    url: jdbc:h2:mem:testdb
    username: sa
    password: 
  # h2数据库配置
  h2:
    console:
      # 是否启用控制台，即可在浏览器图形化查看
      enabled: true
      # 设置访问路径
      path: /h2-console 
